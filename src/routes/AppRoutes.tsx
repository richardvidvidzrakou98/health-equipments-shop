// Routes configuration for the application
// Currently using a single page application structure
// This file can be expanded to include React Router when needed

import Home from '../pages/Home';

export const routes = {
  home: Home,
  // Add more routes here as the application grows
};

export default routes;